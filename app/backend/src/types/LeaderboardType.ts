export type StatisticType = {
  victories: number, // vitorias V
  defeats: number, // derrotas D
  draws: number, // empates  E
};

export type GoalsType = {
  conceded: number, // sofrido GC
  scored: number, // marcado GP
};
